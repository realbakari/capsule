[project]
name = "capsule"
version = "0.1.0"
description = "Agent control plane for governed, replayable execution."
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0"]

[project.scripts]
capsule = "capsule.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
